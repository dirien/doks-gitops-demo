{
  "name": "doks-gitops-demo",
  "version": "1.0.0",
  "description": "AWS EKS cluster with VPC and managed node group",
  "main": "index.ts",
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@pulumi/pulumi": "^3.0.0",
    "@pulumi/aws": "^6.0.0",
    "@pulumi/eks": "^2.0.0"
  }
}
